# Assert there is at least one tag provided
test -z "\$param1$" && echo "-E- tag required." 1>&2 && exit 1

# Assert there is at least one tag provided
test -z "$\xxx" && echo "-E- tag required." 1>&2 && exit 1

# Assert there is at least one tag provided
test -z "$xxx" && echo "-E- tag required." 1>&2 && exit 1

# Assert there is at least one tag provided
test -z "$xxx" && echo "-E- tag required." 1>&2 && exit 1